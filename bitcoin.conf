# Generated by https://jlopp.github.io/bitcoin-core-config-generator/

# This config should be placed in following path:
# ~/.bitcoin/bitcoin.conf

# [core]
bind=172.31.11.40:9333
listen=1
port=9333

# Specify a non-default location to store blockchain data.
blocksdir=/satoshi/data/blocks
# Specify a non-default location to store blockchain and other data.
datadir=/satoshi/data
# Set database cache size in megabytes; machines sync faster with a larger cache. Recommend setting as high as possible based upon machine's available RAM.
dbcache=3000
# Keep at most <n> unconnectable transactions in memory.
maxorphantx=1000
# Keep the transaction memory pool below <n> megabytes.
maxmempool=1000
# Extra transactions to keep in memory for compact block reconstructions.
blockreconstructionextratxn=1000

# Rebuild chain state and block index from the blk*.dat files on disk. WARNING: very slow!
# reindex=1

# Maintain a full transaction index, used by the getrawtransaction rpc call.
txindex=1

# [debug]
# Location of the debug log
debuglogfile=/satoshi/data/debug.log
# Limit size of signature cache to <n> MiB.
maxsigcachesize=100

# [mining]
# Set maximum BIP141 block weight.
blockmaxweight=4000000

# [network]
# Add a node IP address to connect to and attempt to keep the connection open. This option can be set multiple times.
addnode=3.17.82.153:9333
addnode=18.222.44.194:9333


rpcport=8332
rpcbind=172.31.11.40
rpcbind=127.0.0.1
rpcallowip=172.31.11.40

rpcuser=rpcadmin
rpcpassword=juratCryptoE$


# [Sections]
# Most options automatically apply to mainnet, testnet, and regtest networks.
# If you want to confine an option to just one network, you should add it in the relevant section.
# EXCEPTIONS: The options addnode, connect, port, bind, rpcport, rpcbind and wallet
# only apply to mainnet unless they appear in the appropriate section below.

# Options only for mainnet
[main]

# Options only for testnet
[test]

# Options only for regtest
[regtest]
